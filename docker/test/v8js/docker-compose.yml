web:
  build: $PWD/docker/nginx
  ports:
    - 8000:80
  links:
    - fpm:php
  volumes:
    - $PWD/public:/var/public
fpm:
  build: $PWD/docker/phpfpm
  volumes:
    - $PWD/public:/var/public
  ports:
    - 9000:9000
